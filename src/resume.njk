---
name: Colin King-Bailey
title: Software Developer

contact:
    email: ckingbailey@gmail.com
    github: github.com/ckingbailey
    linkedin: linkedin.com/in/ckingbailey
skills:
    - heading: Proficient
      list:
        - JavaScript (ES2015+)
        - Git
        - Node
        - Express
        - CSS3
        - PHP 7
        - MySQL
        - AWS
        - HTML
        - Jest
    - heading: Comfortable
      list:
        - React/Redux
        - D3
        - JQuery
        - Jekyll
        - Sass
        - Mongoose/MongoDB
        - Symfony/Twig
        - TDD/BDD
        - Mocha/Chai/Sinon
        - Tape
        - GCP

experience:
    - company: Freelance
      position: Software Developer
      start_date: March 2018
      end_date: present
      tasks:
        - project: VTA/BART
          long_description: >
            Expedite and create auditable paper trail for finishing phase of construction of BART extension to San Jose
            with a mobile-responsive web application written in PHP, MySQL, Bootstrap, DOM APIs
          tech: 
            - PHP
            - MySQL
            - JavaScript
        - project: Confidential Finance App
          short_description: Equity recommendations and fund analytics
          long_description: Improve investment performance of family office by visualizing stock market analyses
          tech:
            - React
            - Express
            - MySQL
        - project: OUSD Kinder Transitions
          short_description: Kinder promotion automation
          long_description: >-
            Ease the pre-k-to-kinder transition of 400 students, and counting, by automating workflows and inter-teacher communication.
            Gaining more adoption every year, this year the transition web tools will be used by Head Start Oakland in addition to OUSD's Early Learning Lab.
          tech:
            - Google Apps Script
    - company: CoProcure
      position: Software Engineering Intern
      date: Summer 2019
      tasks:
        - Take the guess work out of contractors' code contributions by recommending standardized patterns
        - Save on human labor in ingesting new data sources by automating diverse government website scrapers
        - Double the number of sources ingested by creating data entry workflow and dashboard for management of data entry contractors
        - Ensure the systems I built are bullet-proof with unit tests in Tape and end-to-end tests in Mocha, Chai, Puppeteer

projects:
    - name: Open Disclosure
      tech:
        - Jekyll
        - Ruby
        - React
      short_description: Oakland campaign finance website
      long_description: Increase voter engagement by making it easy to find out where the money comes from in local Oakland elections
    - name: D.A.M.S.S
      tech:
        - React
        - ArcGIS
      short_description: Dams Assessment Mapping Safety System
      long_description: Empower citizens to do something about dams in their area that do not meet maintanence standards

education:
    - institution: San Jose State University
      degree: BFA, Pictorial Art

extra:
    - event: OpenOakland
      activity: Hack Night Lead
    - event: CA Water Data Hackathon 2018
      activity: Winner, Collaboration and Team Spirit Award
    - event: East Bay FreeCodeCamp Meetup
      activity: Organizer, 2017-2018
---
<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <title>CKB Resume</title>
    <link rel="icon" type="image/x-icon" href="favicon.ico">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <style>
        @font-face {
            font-family: 'M PLUS Rounded 1c';
            src: url('/fonts/MPLUSRounded1c-Regular.ttf');
        }

        html, body {
            --purple: #93e;
            --thimble-blue: #2266a7;
            --dark-blue: #0c4099;
            /* --lt-blue: #69d; */
            --thimble-orange: #f15a3b;
            --near-black: #333;
            --dark-grey: #6f6f6f;
            --mid-grey: #999;
            --lt-grey: #ccc;
            --barely-grey: #f6f6f6;
            --base-font: Verdana, Geneva, sans-serif;
            --heading-font: 'M PLUS Rounded 1c', var(--base-font);
            margin: 0;
            padding: 0;
            background-color: var(--barely-grey);
            font-size: 16px;
            font-family: var(--base-font);
            color: var(--near-black);
        }
        h1, h2, h3, h4, h5, h6, address, p {
            margin-top: 0;
            margin-bottom: 0;
        }
        h1, h2, h3, h4, h5, h6 {
            font-weight: normal;
            font-family: var(--heading-font);
        }
        ul {
            margin: 0;
            padding-left: 1.25rem;
            list-style-type: none;
        }
        li::before {
            content: '';
            display: inline-block;
            margin-left: calc(-1 * (6px + .45rem));
            margin-right: .45rem;
            width: 6px; height: 10px;
            background-image: url('data:image/svg+xml;charset=utf8,<svg xmlns="http://www.w3.org/2000/svg" xmlns:xlink="http://www.w3.org/1999/xlink" version="1.1" width="6" height="10"><polygon points="0 2, 6 5, 0 8" fill="%23333"/></svg>');
            background-repeat: no-repeat;
        }
        a {
            text-decoration: none;
            /* color: #249; */
            color: var(--thimble-blue);
            border-color: var(--thimble-blue);
            transition: all 0.2s cubic-bezier(0.4, 0, 0.2, 1);
        }
        a:visited {
            color: var(--dark-blue);
        }
        a:hover {
            color: var(--thimble-orange);
        }
        address {
            font-style: normal;
        }
        ::selection {
            background-color: var(--purple);
            color: white;
        }
        .main-content {
            margin: 0 auto;
            max-width: 8.5in;
            min-height: 100vh;
            background-color: white;
        }
        .resume {
            padding: 1rem .5rem;
        }
        .header, .content-section__content {
            padding-left: .65rem; padding-right: .65rem;
        }
        .header,
        .content-section__content:last-child,
        .content-section__section-heading {
            margin-bottom: 0.5rem;
        }
        .personal-information__title, .content-section__sub-title {
            color: var(--dark-grey);
        }
        .personal-information__title {
            font-size: 1.15rem;
        }
        .personal-information__contact-info-link {
            display: block;
        }
        .content-section {
            text-align: left;
        }
        .content-section__section-heading {
            border-bottom: 2px solid black;
            padding-bottom: .15rem;
            padding-right: .65rem;
            text-align: left;
            font-size: 1.15rem;
            line-height: 1.2;
            color: var(--near-black);
        }
        .content-section__content, .content-section__sub-title, .personal-information__contact-info {
            font-size: .9rem;
        }
        .content-section__sub-title {
            font-family: var(--base-font);
        }
        .personal-information__contact-info,
        .content-section__topic {
            padding-left: .325rem;
        }
        .content-section__topic-heading {
            font-size: .9rem;
            font-family: var(--heading-font);
        }
        .text-grey {
            color: var(--dark-grey);
        }
        @media screen and (min-width: 768px), print {
            ul {
                padding-left: 2.15rem;
            }
            .resume {
                padding: .5in;
            }
            .personal-information, .personal-information__contact-info {
                margin-left: auto; margin-right: auto;
                width: fit-content;
                text-align: center;
            }
            .personal-information__name, .personal-information__title {
                display: inline;
            }
            .personal-information__contact-info-link {
                display: inline;
            }
            .personal-information__contact-info-link:not(:last-child)::after {
                content: '';
                display: inline-block;
                margin-left: .25rem;
                width: 10px;
                height: 10px;
                background-image: url('data:image/svg+xml;charset=utf8,<svg xmlns="http://www.w3.org/2000/svg" xmlns:xlink="http://www.w3.org/1999/xlink" version="1.1" width="10" height="10"><polygon points="3 3, 7 3, 7 7, 3 7" fill="none" stroke-width="1" stroke="%23333" /></svg>');
            }
        }
        @media screen {
        }
        @media print {
            .resume {
                padding: 0;
            }
            a {
                color: var(--dark-blue);
            }
        }
    </style>
</head>
<body>
    <main class="main-content">
        <div id="resume" class="resume">
            <header class="header">
                <div class="personal-information">
                    <h1 id="name" class="personal-information__name">{{ name }}</h1>
                    <h3 id="title" class="personal-information__title">{{ title }}</h3>
                </div>
                <address id="contact_information" class="personal-information__contact-info">
                    <a href="mailto:ckingbailey@gmail.com" class="personal-information__contact-info-link">{{ contact.email }}</a>
                    <a href="https://github.com/ckingbailey" class="personal-information__contact-info-link">{{ contact.github }}</a>
                    <a href="https://www.linkedin.com/in/ckingbailey" class="personal-information__contact-info-link">{{ contact.linkedin }}</a>
                </address>
            </header>
            <section id="skills" class="content-section">
                <h3 class="content-section__section-heading">Skills</h3>
                {% for group in skills %}
                <div class="content-section__content">
                    <p class="content-section__topic">
                        <span class="content-section__topic-heading">{{ group.heading }}: </span>
                        {% set comma = joiner() %}
                        {% for skill in group.list -%}
                            {{ comma() }} {{ skill }}
                        {%- endfor %}
                    </p>
                </div>
                {%- endfor %}
                {# <div class="content-section__content">
                    <p class="content-section__topic">
                        <span class="content-section__topic-heading">Front end:</span>
                        {% set comma = joiner() %}
                        {% for skill in skills.frontend -%}
                            {{ comma() }} {{ skill }}
                        {%- endfor %}
                    </p>
                </div>
                <div class="content-section__content">
                    <p class="content-section__topic">
                        <span class="content-section__topic-heading">Back end:</span>
                        {% set comma = joiner() %}
                        {% for skill in skills.backend -%}
                            {{ comma() }} {{ skill }}
                        {%- endfor %}
                    </p>
                </div>
                <div class="content-section__content">
                    <p class="content-section__topic">
                        <span class="content-section__topic-heading">Other:</span>
                        {% set comma = joiner() %}
                        {% for skill in skills.other -%}
                            {{ comma() }} {{ skill }}
                        {%- endfor %}
                    </p>
                </div> #}
            </section>
            <section id="experience" class="content-section">
                <h3 class="content-section__section-heading">Experience</h3>
                {% for exp in experience -%}
                <div class="content-section__content">
                    <div class="content-section__topic">
                        <span class="content-section__topic-heading">{{ exp.company }}</span>
                        <span>&bull; {{ exp.position }}</span>
                        <h5 class="content-section__sub-title">
                        {% if exp.start_date -%}
                            {{ exp.start_date }} - {{ exp.end_date if exp.end_date else "present" }}
                        {% elif exp.date -%}
                            {{ exp.date }}
                        {%- endif %}
                        </h5>
                    </div>
                    <ul>
                        {% for task in exp.tasks -%}
                        {% if task.project %}
                        <li>
                            {% set comma = joiner() %}
                            <span>{{ task.project }}</span>
                            <span>&bull; {% for tech in task.tech -%}{{ comma() }} {{ tech }}{%- endfor %}</span>
                            {% if task.short_description %}
                            <br><span class="content-section__sub-title">{{ task.short_description }}</span>
                            {% endif %}
                            <br><span>{{task.long_description}}</span></li>
                        {% else%}
                        <li>{{ task }}</li>
                        {% endif %}
                        {%- endfor %}
                    </ul>
                </div>
                {%- endfor %}
            </section>
            <section id="projects" class="content-section">
                <h3 class="content-section__section-heading">Projects</h3>
                {% for proj in projects -%}
                <div class="content-section__content">
                    <p class="content-section__topic">
                        <span class="content-section__topic-heading">{{ proj.name }}</span>
                        {% set comma = joiner() %}
                        <span>&bull; {% for tech in proj.tech -%}{{ comma() }} {{ tech }}{%- endfor %}</span>
                    </p>
                    <div class="content-section__content">
                        <h5 class="content-section__sub-title">{{ proj.short_description }}</h5>
                        <p>{{ proj.long_description }}</p>
                    </div>
                </div>
                {%- endfor %}
            </section>
            <section id="education" class="content-section">
                <h3 class="content-section__section-heading">Education</h3>
                <div class="content-section__content">
                    {% for ed in education -%}
                    <p class="content-section__topic">
                        <span class="content-section__topic-heading">{{ ed.degree }}</span> &bull; {{ ed.institution}}
                    </p>
                    {%- endfor %}
                </div>
            </section>
            <section id="interests" class="content-section">
                <h3 class="content-section__section-heading">Awards and Volunteer Work</h3>
                {% for ex in extra -%}
                <div class="content-section__content">
                    <p class="content-section__topic">
                        <span class="content-section__topic-heading">{{ ex.activity }}</span> &bull; {{ ex.event }}
                    </p>
                </div>
                {%- endfor %}
            </section>
        </div>
    </main>
</body>
</html>